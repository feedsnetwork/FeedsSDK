import BigNumber from 'bignumber.js'

const TAG: string = 'HiveData'

export class HiveData {
  static CommonStatus = {
    available: 0,
    deleted: 1,
    edited: 2,
    sending: 11,
    error: 12,
  }
}
