import {
    Authentication, AppContext
} from "../../../src"

describe("test auth ", () => {

    test("thisAFakeMethod", async () => {
        let commitId = "Hello hotpot";
        expect(commitId).not.toBeNull();
    });
});